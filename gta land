<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GTA V Mobile – Download the Mobile Version</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            width: 100%;
            max-width: 900px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.12), 
                        0 0 1px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            animation: slideUp 0.8s ease-out;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ===== HEADER CAROUSEL ===== */
        .header {
            position: relative;
            padding: 60px 30px;
            text-align: center;
            min-height: 350px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        /* Carousel Container */
        .carousel-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
        }

        /* Carousel Slides */
        .carousel-slide {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            opacity: 0;
            transition: opacity 1s ease-in-out;
        }

        .carousel-slide.active {
            opacity: 1;
        }

        /* Dark Overlay */
        .carousel-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(13, 71, 161, 0.7) 0%, rgba(26, 35, 126, 0.7) 50%, rgba(21, 101, 192, 0.7) 100%);
            z-index: 1;
        }

        /* Neon accent lights */
        .carousel-glow::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(100, 255, 218, 0.15) 0%, transparent 70%);
            animation: float 6s ease-in-out infinite;
            z-index: 2;
        }

        .carousel-glow::after {
            content: '';
            position: absolute;
            bottom: -50%;
            left: -50%;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(100, 200, 255, 0.15) 0%, transparent 70%);
            animation: float 8s ease-in-out infinite reverse;
            z-index: 2;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0); }
            50% { transform: translate(30px, -30px); }
        }

        /* Header Content */
        .header-content {
            position: relative;
            z-index: 3;
        }

        .header h1 {
            font-size: clamp(28px, 8vw, 48px);
            font-weight: 900;
            color: #ffffff;
            text-shadow: 0 4px 20px rgba(100, 255, 218, 0.4),
                         0 0 30px rgba(100, 200, 255, 0.3),
                         0 0 10px rgba(0, 0, 0, 0.5);
            letter-spacing: -1px;
            margin-bottom: 10px;
            line-height: 1.2;
            animation: textGlow 3s ease-in-out infinite;
        }

        @keyframes textGlow {
            0%, 100% { text-shadow: 0 4px 20px rgba(100, 255, 218, 0.4), 0 0 30px rgba(100, 200, 255, 0.3), 0 0 10px rgba(0, 0, 0, 0.5); }
            50% { text-shadow: 0 4px 30px rgba(100, 255, 218, 0.6), 0 0 40px rgba(100, 200, 255, 0.5), 0 0 15px rgba(0, 0, 0, 0.6); }
        }

        .header p {
            font-size: clamp(14px, 3vw, 18px);
            color: rgba(255, 255, 255, 0.95);
            font-weight: 300;
            letter-spacing: 0.5px;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
        }

        /* Carousel Navigation Arrows */
        .carousel-nav {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            z-index: 4;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: none;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            font-size: 24px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .carousel-nav:hover {
            background: rgba(255, 255, 255, 0.4);
            transform: translateY(-50%) scale(1.1);
        }

        .carousel-nav:active {
            transform: translateY(-50%) scale(0.95);
        }

        .carousel-prev {
            left: 20px;
        }

        .carousel-next {
            right: 20px;
        }

        /* Carousel Indicators (Dots) */
        .carousel-indicators {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 4;
            display: flex;
            gap: 12px;
        }

        .indicator-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.4);
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.2);
        }

        .indicator-dot.active {
            background: rgba(255, 255, 255, 0.9);
            transform: scale(1.3);
            box-shadow: 0 0 15px rgba(100, 255, 218, 0.6);
        }

        .indicator-dot:hover {
            background: rgba(255, 255, 255, 0.7);
        }

        /* Content Section */
        .content {
            padding: 60px 30px;
        }

        /* GTA V Artwork */
        .artwork-container {
            text-align: center;
            margin-bottom: 50px;
            animation: slideDown 0.8s ease-out 0.2s both;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .artwork {
            max-width: 100%;
            height: auto;
            width: 100%;
            max-width: 500px;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(13, 71, 161, 0.2);
            transition: transform 0.3s ease;
        }

        .artwork:hover {
            transform: scale(1.02);
        }

        /* Features Section */
        .features-section {
            margin-bottom: 50px;
            animation: slideDown 0.8s ease-out 0.4s both;
        }

        .features-title {
            font-size: clamp(20px, 5vw, 28px);
            font-weight: 700;
            color: #0d47a1;
            text-align: center;
            margin-bottom: 30px;
            letter-spacing: -0.5px;
        }

        .features-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .feature-item {
            display: flex;
            align-items: flex-start;
            gap: 15px;
            padding: 20px;
            background: linear-gradient(135deg, rgba(13, 71, 161, 0.05) 0%, rgba(21, 101, 192, 0.05) 100%);
            border-radius: 12px;
            border-left: 4px solid #1565c0;
            transition: all 0.3s ease;
        }

        .feature-item:hover {
            transform: translateX(5px);
            box-shadow: 0 8px 20px rgba(21, 101, 192, 0.15);
            border-left-color: #64ffda;
        }

        .checkmark {
            flex-shrink: 0;
            width: 28px;
            height: 28px;
            background: linear-gradient(135deg, #1565c0 0%, #0d47a1 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 16px;
            box-shadow: 0 4px 12px rgba(21, 101, 192, 0.3);
        }

        .feature-text {
            flex: 1;
        }

        .feature-text h3 {
            font-size: 16px;
            font-weight: 700;
            color: #0d47a1;
            margin-bottom: 5px;
        }

        .feature-text p {
            font-size: 14px;
            color: #666;
            line-height: 1.5;
        }

        /* Download Buttons Section */
        .buttons-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-top: 50px;
            animation: slideDown 0.8s ease-out 0.6s both;
        }

        .download-btn {
            padding: 18px 40px;
            font-size: 16px;
            font-weight: 700;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            text-decoration: none;
            color: white;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
            letter-spacing: 0.5px;
        }

        .download-btn:active {
            transform: scale(0.98);
        }

        /* Android Button */
        .btn-android {
            background: linear-gradient(135deg, #34c759 0%, #30b247 100%);
            color: white;
        }

        .btn-android:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(52, 199, 89, 0.35);
        }

        /* iOS Button */
        .btn-ios {
            background: linear-gradient(135deg, #1c1c1c 0%, #3a3a3a 100%);
            color: white;
        }

        .btn-ios:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.35);
        }

        /* Icon styling for buttons */
        .btn-icon {
            font-size: 20px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                border-radius: 16px;
            }

            .header {
                padding: 40px 20px;
                min-height: 300px;
            }

            .header h1 {
                font-size: 32px;
            }

            .carousel-nav {
                width: 40px;
                height: 40px;
                font-size: 18px;
            }

            .carousel-prev {
                left: 10px;
            }

            .carousel-next {
                right: 10px;
            }

            .content {
                padding: 40px 20px;
            }

            .features-list {
                grid-template-columns: 1fr;
            }

            .buttons-section {
                grid-template-columns: 1fr;
            }

            .download-btn {
                width: 100%;
            }
        }

        @media (max-width: 480px) {
            .header {
                padding: 30px 15px;
                min-height: 250px;
            }

            .header h1 {
                font-size: 24px;
            }

            .header p {
                font-size: 14px;
            }

            .carousel-nav {
                width: 36px;
                height: 36px;
                font-size: 16px;
            }

            .carousel-prev {
                left: 5px;
            }

            .carousel-next {
                right: 5px;
            }

            .content {
                padding: 30px 15px;
            }

            .features-title {
                font-size: 22px;
            }

            .feature-item {
                padding: 16px;
            }

            .download-btn {
                padding: 16px 30px;
                font-size: 14px;
            }

            .indicator-dot {
                width: 10px;
                height: 10px;
            }
        }

        /* Loading animation for page */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        html {
            animation: fadeIn 0.5s ease-out;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header with Image Carousel -->
        <div class="header">
            <!-- Carousel Container -->
            <div class="carousel-container">
                <!-- Slide 1 -->
                <div class="carousel-slide active" style="background-image: url('https://i.imgur.com/Pj06LFJ.jpg');"></div>
                
                <!-- Slide 2 -->
                <div class="carousel-slide" style="background-image: url('https://i.imgur.com/riTSKsx.jpg');"></div>
                
                <!-- Slide 3 -->
                <div class="carousel-slide" style="background-image: url('https://i.imgur.com/jqBbc1c.jpg');"></div>

                <!-- Dark Overlay -->
                <div class="carousel-overlay"></div>

                <!-- Neon Glow Effect -->
                <div class="carousel-glow"></div>
            </div>

            <!-- Navigation Arrows -->
            <button class="carousel-nav carousel-prev" onclick="prevSlide()">❮</button>
            <button class="carousel-nav carousel-next" onclick="nextSlide()">❯</button>

            <!-- Indicator Dots -->
            <div class="carousel-indicators">
                <span class="indicator-dot active" onclick="goToSlide(0)"></span>
                <span class="indicator-dot" onclick="goToSlide(1)"></span>
                <span class="indicator-dot" onclick="goToSlide(2)"></span>
            </div>

            <!-- Header Content -->
            <div class="header-content">
                <h1>GTA V Mobile</h1>
                <p>Download the Mobile Version</p>
            </div>
        </div>

        <!-- Content Section -->
        <div class="content">
            <!-- GTA V Artwork -->
            <div class="artwork-container">
                <img src="https://imgs.search.brave.com/uj5K8BpBfreNXHemUfeKU4STathgOqEqFpKU63QhbtM/rs:fit:860:0:0:0/g:ce/aHR0cHM6Ly9pLnBp/bmltZy5jb20vb3Jp/Z2luYWxzL2IxLzY2/LzJkL2IxNjYyZDdm/Y2UwYzhiNjYxYjEw/MzliYzA3YWRhMjRk/LmpwZw" alt="GTA V Mobile - Logo" class="artwork">
            </div>

            <!-- Features Section -->
            <div class="features-section">
                <h2 class="features-title">Why Choose GTA V Mobile?</h2>
                <div class="features-list">
                    <div class="feature-item">
                        <div class="checkmark">✓</div>
                        <div class="feature-text">
                            <h3>Optimized Performance</h3>
                            <p>Smooth gameplay with lightning-fast frame rates on all mobile devices</p>
                        </div>
                    </div>

                    <div class="feature-item">
                        <div class="checkmark">✓</div>
                        <div class="feature-text">
                            <h3>HD Graphics</h3>
                            <p>Stunning visuals with advanced rendering technology for mobile</p>
                        </div>
                    </div>

                    <div class="feature-item">
                        <div class="checkmark">✓</div>
                        <div class="feature-text">
                            <h3>Open World Gameplay</h3>
                            <p>Experience the full GTA V universe in the palm of your hand</p>
                        </div>
                    </div>

                    <div class="feature-item">
                        <div class="checkmark">✓</div>
                        <div class="feature-text">
                            <h3>Fast Installation</h3>
                            <p>Quick setup process with minimal storage requirements</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Download Buttons -->
            <div class="buttons-section">
                <button class="download-btn btn-android" onclick="handleDownload('Android')">
                    <span class="btn-icon">▶</span>
                    <span>Download Android</span>
                </button>
                <button class="download-btn btn-ios" onclick="handleDownload('iOS')">
                    <span class="btn-icon">▶</span>
                    <span>Download iOS</span>
                </button>
            </div>
        </div>
    </div>

    <script>
        // ===== CAROUSEL FUNCTIONALITY =====
        let currentSlide = 0;
        const slides = document.querySelectorAll('.carousel-slide');
        const indicators = document.querySelectorAll('.indicator-dot');
        const totalSlides = slides.length;
        let autoRotateTimer;

        function showSlide(index) {
            // Remove active class from all slides and indicators
            slides.forEach(slide => slide.classList.remove('active'));
            indicators.forEach(dot => dot.classList.remove('active'));

            // Add active class to current slide and indicator
            slides[index].classList.add('active');
            indicators[index].classList.add('active');

            currentSlide = index;

            // Reset auto-rotate timer
            clearTimeout(autoRotateTimer);
            autoRotateTimer = setTimeout(() => {
                nextSlide();
            }, 5000); // Auto-rotate every 5 seconds
        }

        function nextSlide() {
            const nextIndex = (currentSlide + 1) % totalSlides;
            showSlide(nextIndex);
        }

        function prevSlide() {
            const prevIndex = (currentSlide - 1 + totalSlides) % totalSlides;
            showSlide(prevIndex);
        }

        function goToSlide(index) {
            showSlide(index);
        }

        // Initialize carousel with auto-rotate
        autoRotateTimer = setTimeout(() => {
            nextSlide();
        }, 5000);

        // Add smooth scroll behavior
        document.documentElement.style.scrollBehavior = 'smooth';

        // Handle download button clicks
        function handleDownload(platform) {
            console.log(`Download ${platform} initiated`);
            // Replace these URLs with your actual download links
            const downloadLinks = {
                'Android': 'https://play.google.com/store/apps/details?id=com.gta.v',
                'iOS': 'https://apps.apple.com/app/gta-v'
            };
            
            // Uncomment to open download link:
            // window.open(downloadLinks[platform], '_blank');
        }

        // Add ripple effect on button click
        document.querySelectorAll('.download-btn').forEach(btn => {
            btn.addEventListener('click', function(e) {
                const ripple = document.createElement('span');
                const rect = this.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                const x = e.clientX - rect.left - size / 2;
                const y = e.clientY - rect.top - size / 2;

                ripple.style.position = 'absolute';
                ripple.style.width = ripple.style.height = size + 'px';
                ripple.style.left = x + 'px';
                ripple.style.top = y + 'px';
                ripple.style.background = 'rgba(255, 255, 255, 0.6)';
                ripple.style.borderRadius = '50%';
                ripple.style.pointerEvents = 'none';
                ripple.style.animation = 'ripple-animation 0.6s ease-out';

                const style = document.createElement('style');
                style.textContent = `
                    @keyframes ripple-animation {
                        to {
                            transform: scale(4);
                            opacity: 0;
                        }
                    }
                `;
                if (!document.querySelector('style[data-ripple]')) {
                    style.setAttribute('data-ripple', 'true');
                    document.head.appendChild(style);
                }

                this.style.position = 'relative';
                this.style.overflow = 'hidden';
                this.appendChild(ripple);

                setTimeout(() => ripple.remove(), 600);
            });
        });
    </script>
</body>
</html>
